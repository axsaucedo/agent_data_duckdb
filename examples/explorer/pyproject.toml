[project]
name = "agent-data-explorer"
version = "0.1.0"
description = "Streamlit explorer for AI agent session data via the agent_data DuckDB extension"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.45",
    "duckdb>=1.4",
    "pandas>=2.0",
]
